{
  "name": "riannegreiros",
  "version": "1.0.0",
  "description": "Rian Negreiros's GitHub Profile",
  "main": "src/app.ts",
  "scripts": {
    "build": "npx tsc",
    "build:watch": "npx tsc-watch",
    "start": "npm run build && node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:fix": "prettier --write ."
  },
  "dependencies": {
    "markdown-it": "^14.0.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.10.5",
    "prettier": "^3.2.5",
    "tsc-watch": "^6.0.4"
  }
}